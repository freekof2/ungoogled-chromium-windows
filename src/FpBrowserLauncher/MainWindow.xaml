<Window x:Class="FpBrowserLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FpBrowserLauncher.Converters"
        Title="Fingerprint Browser Launcher" Height="650" Width="1040"
        MinHeight="560" MinWidth="900"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <local:StringToBoolConverter x:Key="StringToBoolConverter" />
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Chromium 路径" VerticalAlignment="Center" Margin="0,0,8,0" Style="{StaticResource LabelStyle}" />
                <TextBox Grid.Column="1" Text="{Binding ChromiumPath, UpdateSourceTrigger=PropertyChanged}" MinWidth="520" Style="{StaticResource ModernTextBoxStyle}" />
                <Button Grid.Column="2" Content="保存设置" Command="{Binding SaveSettingsCommand}" Margin="8,0,0,0" Padding="12,4" Style="{StaticResource ModernButtonStyle}" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,16,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="236" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="刷新" Command="{Binding RefreshCommand}" Padding="12,4" Style="{StaticResource ModernButtonStyle}" />
                    <Button Content="删除" Command="{Binding DeleteProfileCommand}" Margin="8,0,0,0" Padding="12,4" Style="{StaticResource ModernButtonStyle}" />
                </StackPanel>
                <ListView ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="DisplayText" />
            </DockPanel>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button Content="创建/更新并生成" Command="{Binding SaveProfileCommand}" Padding="12,4" Style="{StaticResource ModernButtonStyle}" />
                        <Button Content="启动" Command="{Binding LaunchProfileCommand}" Margin="8,0,0,0" Padding="12,4" Style="{StaticResource ModernButtonStyle}" />
                        <Button Content="停止" Command="{Binding StopProfileCommand}" Margin="8,0,0,0" Padding="12,4" Style="{StaticResource ModernButtonStyle}" />
                    </StackPanel>

                    <TabControl>
                        <TabItem Header="基础">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Profile ID" VerticalAlignment="Center" Style="{StaticResource LabelStyle}" />
                                <TextBox Grid.Column="1" Text="{Binding ProfileId, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" />
                                <TextBlock Grid.Row="1" Text="显示名称" VerticalAlignment="Center" Margin="0,8,0,0" Style="{StaticResource LabelStyle}" />
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0" Style="{StaticResource ModernTextBoxStyle}" />
                                <TextBlock Grid.Row="2" Text="代理 Host" VerticalAlignment="Center" Margin="0,8,0,0" Style="{StaticResource LabelStyle}" />
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ProxyHost, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0" Style="{StaticResource ModernTextBoxStyle}" />
                                <TextBlock Grid.Row="3" Text="代理 Port" VerticalAlignment="Center" Margin="0,8,0,0" Style="{StaticResource LabelStyle}" />
                                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ProxyPortText, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0" Style="{StaticResource ModernTextBoxStyle}" />
                                <TextBlock Grid.Row="4" Text="代理出口 IP" VerticalAlignment="Center" Margin="0,8,0,0" Style="{StaticResource LabelStyle}" />
                                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding ProxyPublicIp, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0" Style="{StaticResource ModernTextBoxStyle}" />
                                <TextBlock Grid.Row="5" Text="认证" VerticalAlignment="Top" Margin="0,8,0,0" Style="{StaticResource LabelStyle}" />
                                <Grid Grid.Row="5" Grid.Column="1" Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="8" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Text="{Binding ProxyUsername, UpdateSourceTrigger=PropertyChanged}" ToolTip="用户名" Style="{StaticResource ModernTextBoxStyle}" />
                                    <TextBox Grid.Column="2" Text="{Binding ProxyPassword, UpdateSourceTrigger=PropertyChanged}" ToolTip="密码" Style="{StaticResource ModernTextBoxStyle}" />
                                </Grid>
                                <TextBlock Grid.Row="6" Text="User-Agent" VerticalAlignment="Center" Margin="0,8,0,0" Style="{StaticResource LabelStyle}" />
                                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding UserAgent, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,0" Style="{StaticResource ModernTextBoxStyle}" />
                                <Border Grid.Row="7" Grid.ColumnSpan="2" Style="{StaticResource GreyCardStyle}" Margin="0,16,0,0">
                                    <TextBox Text="{Binding SelectedProfileDetails, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="Transparent" BorderThickness="0" />
                                </Border>
                            </Grid>
                        </TabItem>

                        <TabItem Header="指纹设置">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel Margin="8">
                                    <!-- WebRTC 模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="WebRTC" Style="{StaticResource SectionTitleStyle}" />
                                            <ComboBox ItemsSource="{Binding WebRtcModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding WebRtcMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                        </StackPanel>
                                    </Border>

                                    <!-- 时区模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="时区" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="130" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <ComboBox ItemsSource="{Binding TimezoneModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding TimezoneMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <ComboBox Grid.Column="2" IsEditable="True" ItemsSource="{Binding TimezoneValueOptions}" Text="{Binding TimezoneValue, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- 地理位置模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="地理位置" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="110" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="110" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <ComboBox ItemsSource="{Binding GeolocationModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding GeolocationMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <TextBox Grid.Column="2" Text="{Binding GeolocationLatitude}" ToolTip="纬度" Style="{StaticResource ModernTextBoxStyle}" />
                                                <TextBox Grid.Column="4" Text="{Binding GeolocationLongitude}" ToolTip="经度" Style="{StaticResource ModernTextBoxStyle}" />
                                                <TextBox Grid.Column="6" Text="{Binding GeolocationAccuracy}" ToolTip="精度米" Style="{StaticResource ModernTextBoxStyle}" />
                                                <Button Grid.Column="8" Content="↻" Command="{Binding RandomizeGeolocationCommand}" Width="32" Height="32" ToolTip="随机地理位置" Style="{StaticResource ModernButtonStyle}" />
                                            </Grid>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="授权策略：" Style="{StaticResource LabelStyle}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                                <ComboBox ItemsSource="{Binding GeolocationPromptPolicyOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding GeolocationPromptPolicy, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" Style="{StaticResource ModernComboBoxStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- 语言模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="语言" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="130" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <ComboBox ItemsSource="{Binding LanguageModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding LanguageMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <TextBox Grid.Column="2" Text="{Binding LanguagesText}" ToolTip="逗号分隔，如 en-US,en" Style="{StaticResource ModernTextBoxStyle}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- 界面语言模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="界面语言" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="130" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="160" />
                                                </Grid.ColumnDefinitions>
                                                <ComboBox ItemsSource="{Binding UiLanguageModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding UiLanguageMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <TextBox Grid.Column="2" Text="{Binding UiLanguageValue}" Style="{StaticResource ModernTextBoxStyle}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- 分辨率模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="分辨率" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="130" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="90" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="90" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <ComboBox ItemsSource="{Binding ResolutionModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding ResolutionMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <TextBox Grid.Column="2" Text="{Binding ResolutionWidth}" Style="{StaticResource ModernTextBoxStyle}" />
                                                <TextBox Grid.Column="4" Text="{Binding ResolutionHeight}" Style="{StaticResource ModernTextBoxStyle}" />
                                                <Button Grid.Column="6" Content="↻" Command="{Binding RandomizeResolutionCommand}" Width="32" Height="32" ToolTip="随机分辨率" Style="{StaticResource ModernButtonStyle}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- 字体模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="字体" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="100" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <ComboBox ItemsSource="{Binding FontsModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding FontsMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <TextBox Grid.Column="2" Text="{Binding FontsText}" Style="{StaticResource ModernTextBoxStyle}" />
                                                <Button Grid.Column="4" Content="换一换" Command="{Binding RandomizeFontsCommand}" Padding="8,0" ToolTip="随机字体组合" Style="{StaticResource ModernButtonStyle}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- 硬件噪声模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="硬件噪声" Style="{StaticResource SectionTitleStyle}" />
                                            <WrapPanel>
                                                <CheckBox Content="Canvas" IsChecked="{Binding CanvasNoise}" Margin="0,0,12,0" Style="{StaticResource SwitchStyle}" />
                                                <CheckBox Content="WebGL图像" IsChecked="{Binding WebGlImageNoise}" Margin="0,0,12,0" Style="{StaticResource SwitchStyle}" />
                                                <CheckBox Content="AudioContext" IsChecked="{Binding AudioContextNoise}" Margin="0,0,12,0" Style="{StaticResource SwitchStyle}" />
                                                <CheckBox Content="媒体设备" IsChecked="{Binding MediaDevicesNoise}" Margin="0,0,12,0" Style="{StaticResource SwitchStyle}" />
                                                <CheckBox Content="ClientRects" IsChecked="{Binding ClientRectsNoise}" Margin="0,0,12,0" Style="{StaticResource SwitchStyle}" />
                                                <CheckBox Content="SpeechVoices" IsChecked="{Binding SpeechVoicesNoise}" Style="{StaticResource SwitchStyle}" />
                                            </WrapPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- WebGL 模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="WebGL" Style="{StaticResource SectionTitleStyle}" />
                                            <ComboBox ItemsSource="{Binding WebGlModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding WebGlMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" Style="{StaticResource ModernComboBoxStyle}" />
                                            <TextBox Text="{Binding WebGlVendor}" Margin="0,0,0,8" Style="{StaticResource ModernTextBoxStyle}" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox Text="{Binding WebGlRenderer}" Style="{StaticResource ModernTextBoxStyle}" />
                                                <Button Grid.Column="2" Content="↻" Command="{Binding RandomizeWebGlCommand}" Width="32" Height="32" ToolTip="随机 WebGL 元数据" Style="{StaticResource ModernButtonStyle}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- WebGPU/CPU/RAM 模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="WebGPU / CPU / RAM" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="130" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="120" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <ComboBox ItemsSource="{Binding WebGpuModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding WebGpuMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <ComboBox Grid.Column="2" IsEditable="True" ItemsSource="{Binding CpuCoreOptions}" Text="{Binding CpuCores, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <ComboBox Grid.Column="4" IsEditable="True" ItemsSource="{Binding MemoryOptions}" Text="{Binding DeviceMemoryGb, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <Button Grid.Column="6" Content="↻" Command="{Binding RandomizeCpuMemoryCommand}" Width="32" Height="32" ToolTip="随机 CPU/RAM" Style="{StaticResource ModernButtonStyle}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- 设备标识模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="设备标识" Style="{StaticResource SectionTitleStyle}" />
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox Text="{Binding DeviceName}" Style="{StaticResource ModernTextBoxStyle}" />
                                                <Button Grid.Column="2" Content="↻" Command="{Binding RandomizeDeviceNameCommand}" Width="32" Height="32" ToolTip="随机设备名称" Style="{StaticResource ModernButtonStyle}" />
                                            </Grid>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="8" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBox Text="{Binding MacAddress}" Style="{StaticResource ModernTextBoxStyle}" />
                                                <Button Grid.Column="2" Content="↻" Command="{Binding RandomizeMacAddressCommand}" Width="32" Height="32" ToolTip="随机 MAC 地址" Style="{StaticResource ModernButtonStyle}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <!-- DNT / 端口扫描保护模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="DNT / 端口扫描保护" Style="{StaticResource SectionTitleStyle}" />
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                <ComboBox Width="120" ItemsSource="{Binding DoNotTrackOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding DoNotTrack, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <TextBlock Text="端口扫描保护" Style="{StaticResource LabelStyle}" VerticalAlignment="Center" Margin="8,0,8,0" />
                                                <ComboBox Width="100" ItemsSource="{Binding EnabledDisabledOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding PortScanProtectionEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <TextBox Width="220" Margin="8,0,0,0" Text="{Binding AllowedPorts}" ToolTip="允许端口，逗号分隔" Style="{StaticResource ModernTextBoxStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- 硬件加速 / TLS 模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="硬件加速 / TLS 指纹" Style="{StaticResource SectionTitleStyle}" />
                                            <StackPanel Orientation="Horizontal">
                                                <ComboBox Width="130" ItemsSource="{Binding HardwareAccelerationOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding HardwareAcceleration, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                                <ComboBox Width="180" Margin="8,0,0,0" ItemsSource="{Binding TlsFingerprintModeOptions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding TlsFingerprintMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- 启动参数模块 -->
                                    <Border Style="{StaticResource GreyCardStyle}">
                                        <StackPanel>
                                            <TextBlock Text="启动参数" Style="{StaticResource SectionTitleStyle}" />
                                            <TextBox Height="80" Text="{Binding ExtraFlagsText}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernTextBoxStyle}" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>
                    </TabControl>
                </DockPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="{StaticResource LightTextBrush}" />
    </Grid>
</Window>
