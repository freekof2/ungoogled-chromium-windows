# Fingerprint browser: deterministic noise injection with safe bounds.
--- /dev/null
+++ b/base/fp_config/fp_noise.h
@@ -0,0 +1,41 @@
+// Copyright 2026 The Chromium Authors
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+#ifndef BASE_FP_CONFIG_FP_NOISE_H_
+#define BASE_FP_CONFIG_FP_NOISE_H_
+
+#include <stddef.h>
+#include <stdint.h>
+
+#include <algorithm>
+
+#include "base/fp_config/fp_config.h"
+
+namespace fp_noise {
+
+inline uint8_t ByteNoise(uint64_t salt) {
+  return static_cast<uint8_t>(fp_config::StableNoiseUnit(salt) * 3.0);
+}
+
+inline void InjectBytes(uint8_t* data, size_t size, uint64_t salt) {
+  if (!data || size == 0) {
+    return;
+  }
+  for (size_t i = 0; i < size; i += 97) {
+    uint8_t noise = ByteNoise(salt + i);
+    data[i] = static_cast<uint8_t>(std::clamp<int>(data[i] + noise - 1, 0, 255));
+  }
+}
+
+inline float InjectFloat(float value, uint64_t salt, float amplitude) {
+  return value + static_cast<float>(fp_config::StableNoise(salt, amplitude));
+}
+
+inline double InjectDouble(double value, uint64_t salt, double amplitude) {
+  return value + fp_config::StableNoise(salt, amplitude);
+}
+
+}  // namespace fp_noise
+
+#endif  // BASE_FP_CONFIG_FP_NOISE_H_
--- a/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
+++ b/third_party/blink/renderer/modules/webgl/webgl_rendering_context_base.cc
@@ -34,6 +34,8 @@
 #include "base/byte_size.h"
 #include "base/compiler_specific.h"
 #include "base/feature_list.h"
+#include "base/fp_config/fp_config.h"
+#include "base/fp_config/fp_noise.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
@@ -5346,6 +5348,16 @@
       return;
     }
     ContextGL()->ReadPixels(x, y, width, height, format, type, data);
+    if (fp_config::NoiseEnabled("webgl_image") && data) {
+      base::CheckedNumeric<size_t> noise_size = width;
+      noise_size *= height;
+      noise_size *= 4;
+      if (noise_size.IsValid()) {
+        const size_t safe_size = std::min(
+            noise_size.ValueOrDie(), static_cast<size_t>(buffer_size.ValueOrDie()));
+        fp_noise::InjectBytes(data, safe_size, 0x57474c);
+      }
+    }
   }
 }
 
--- a/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
+++ b/third_party/blink/renderer/modules/canvas/canvas2d/base_rendering_context_2d.cc
@@ -14,6 +14,8 @@
 
 #include "base/check.h"
 #include "base/check_op.h"
+#include "base/fp_config/fp_config.h"
+#include "base/fp_config/fp_noise.h"
 #include "base/location.h"
 #include "base/memory/scoped_refptr.h"
 #include "base/memory/weak_ptr.h"
@@ -512,6 +514,10 @@
       DCHECK(!bounds.intersect(SkIRect::MakeXYWH(sx, sy, sw, sh)));
     }
   }
+  if (fp_config::NoiseEnabled("canvas") && image_data) {
+    auto data = image_data->RawByteSpan();
+    fp_noise::InjectBytes(data.data(), data.size(), 0x43414e564153ull);
+  }
 
   return image_data;
 }
--- a/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
+++ b/third_party/blink/renderer/modules/webaudio/offline_audio_context.cc
@@ -27,6 +27,8 @@
 
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/histogram_macros.h"
+#include "base/fp_config/fp_config.h"
+#include "base/fp_config/fp_noise.h"
 #include "media/base/audio_glitch_info.h"
 #include "third_party/blink/public/platform/platform.h"
 #include "third_party/blink/renderer/bindings/core/v8/script_promise_resolver.h"
@@ -435,6 +437,16 @@
     // Call the offline rendering completion event listener and resolve the
     // promise too.
     DispatchEvent(*OfflineAudioCompletionEvent::Create(rendered_buffer));
+    if (fp_config::NoiseEnabled("audio_context")) {
+      for (unsigned channel = 0; channel < rendered_buffer->numberOfChannels();
+           ++channel) {
+        NotShared<DOMFloat32Array> data = rendered_buffer->getChannelData(channel);
+        for (unsigned i = 0; i < data->length(); i += 211) {
+          data->Data()[i] = fp_noise::InjectFloat(
+              data->Data()[i], 0x415544494full + i + channel, 0.0000003f);
+        }
+      }
+    }
     complete_resolver_->Resolve(rendered_buffer);
   } else {
     // The resolver should be rejected when the execution context is gone.
--- a/third_party/blink/renderer/core/dom/element.cc
+++ b/third_party/blink/renderer/core/dom/element.cc
@@ -33,6 +33,8 @@
 #include <utility>
 
 #include "base/containers/adapters.h"
+#include "base/fp_config/fp_config.h"
+#include "base/fp_config/fp_noise.h"
 #include "base/feature_list.h"
 #include "cc/input/snap_selection_strategy.h"
 #include "components/viz/common/surfaces/tracked_element_rects.h"
@@ -3515,7 +3517,14 @@
 DOMRect* Element::GetBoundingClientRect() {
   GetDocument().EnsurePaintLocationDataValidForNode(
       this, DocumentUpdateReason::kJavaScript);
-  return DOMRect::FromRectF(GetBoundingClientRectNoLifecycleUpdate());
+  gfx::RectF result = GetBoundingClientRectNoLifecycleUpdate();
+  if (fp_config::NoiseEnabled("client_rects")) {
+    result.set_x(fp_noise::InjectFloat(result.x(), 0x43525801, 0.02f));
+    result.set_y(fp_noise::InjectFloat(result.y(), 0x43525802, 0.02f));
+    result.set_width(fp_noise::InjectFloat(result.width(), 0x43525803, 0.02f));
+    result.set_height(fp_noise::InjectFloat(result.height(), 0x43525804, 0.02f));
+  }
+  return DOMRect::FromRectF(result);
 }
 
 DOMRect* Element::GetBoundingClientRectForBinding() {
--- a/third_party/blink/renderer/modules/speech/speech_synthesis.cc
+++ b/third_party/blink/renderer/modules/speech/speech_synthesis.cc
@@ -28,6 +28,7 @@
 #include <tuple>
 
 #include "build/build_config.h"
+#include "base/fp_config/fp_config.h"
 #include "third_party/blink/public/common/thread_safe_browser_interface_broker_proxy.h"
 #include "third_party/blink/public/platform/browser_interface_broker_proxy.h"
 #include "third_party/blink/public/platform/platform.h"
@@ -88,6 +89,11 @@
   for (auto& mojom_voice : mojom_voices) {
     voice_list_.push_back(
         MakeGarbageCollected<SpeechSynthesisVoice>(std::move(mojom_voice)));
+  }
+  if (fp_config::NoiseEnabled("speech_voices") && voice_list_.size() > 4) {
+    while (voice_list_.size() > 4) {
+      voice_list_.pop_back();
+    }
   }
   VoicesDidChange();
 }
--- a/third_party/blink/renderer/modules/mediastream/media_devices.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_devices.cc
@@ -3,6 +3,7 @@
 // found in the LICENSE file.
 
 #include "third_party/blink/renderer/modules/mediastream/media_devices.h"
+#include "base/fp_config/fp_config.h"
 
 #include <algorithm>
 #include <utility>
@@ -525,7 +526,17 @@
       script_state, "Media.MediaDevices.EnumerateDevices",
       kEnumerateDevicesTimeout);
   const auto promise = result_tracker->Promise();
-
+  if (fp_config::NoiseEnabled("media_devices")) {
+    HeapVector<Member<MediaDeviceInfo>> devices;
+    devices.push_back(MakeGarbageCollected<MediaDeviceInfo>(
+        "fp-audio-input", "Default Microphone", "fp-group",
+        mojom::blink::MediaDeviceType::kMediaAudioInput));
+    devices.push_back(MakeGarbageCollected<MediaDeviceInfo>(
+        "fp-video-input", "Default Camera", "fp-group",
+        mojom::blink::MediaDeviceType::kMediaVideoInput));
+    result_tracker->Resolve(devices, EnumerateDevicesResult::kOk);
+    return promise;
+  }
   SendLogMessage(base::StringPrintf(
       "enumerateDevices({result_tracker=%p}, "
       "{request_audio_input_capabilities=%s})",
